.block
  .hd
    %h2 Import students from file
  .bd
    - semantic_form_for @student_import, :html => { :multipart => true } do |f|
      = f.input :file, :as => :file, :hint => 'only YAML'
      - f.buttons do
        = f.commit_button "Commit"